<div id="app">
  <section class="section">
    <h1 class="title">Guard App</h1>
    <h2 class="subtitle">
      Las tablas mostradas solo listan los datos, no permiten edición. Para editar vaya más abajo e indique los rangos
      de tiempo en el formulario y haga click en el botón Guardar.
    </h2>
    <br>
    <div class="columns">
      <div class="column is-four-fifths">
        <week-schedule-summary :summary="<%= @summary %>" :employee-colors="<%= @employee_colors %>"></week-schedule-summary>
      </div>
      <div class="column">
        <%= link_to '<- Vista Principal', root_path, class: 'button' %>
      </div>
    </div>
    <br>
    <week-schedule :schedule="<%= @schedule %>" :employee-colors="<%= @employee_colors %>"></week-schedule>
    <hr>
    <%= render "form" %>
  </section>
</div>